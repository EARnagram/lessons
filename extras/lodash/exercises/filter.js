const _           = require('lodash');
const skateboards = require('../data');

// Use a function to find only long boards
var longBoards;

// Use a string to find only flat boards
var flatBoards;

// Use an object to find only curved, short boards
var trickBoards;

// Use an array to find only curved boards
var curvedBoards;

console.log("LONGBOARDS: ",   longBoards);
console.log("FLATBOARDS: ",   flatBoards);
console.log("TRICKBOARDS: ",  trickBoards);
console.log("CURVEDBOARDS: ", curvedBoards);
