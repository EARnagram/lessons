const _           = require('lodash');
const skateboards = require('../data');

// Sort by curved, then stretch
var curvedShortFirst;

// Sort by length of name, with long names first
var longNamesFirst;

console.log(curvedShortFirst);

console.log(longNamesFirst);
