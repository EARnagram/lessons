const _           = require('lodash');
const skateboards = require('../data');

// List the stretches by the their value rounded down (eg. 5.8 => 5).
var lengthsChart;

// Write your own function to separate "flat" or "curved" boards in an
// object.
var flatCurved;

console.log("Floored Length: ", lengthsChart);

console.log("Flat or Curved: ", flatCurved);
